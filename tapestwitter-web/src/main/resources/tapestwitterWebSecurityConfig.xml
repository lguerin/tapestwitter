<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
			xmlns:beans="http://www.springframework.org/schema/beans"
			xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
			xsi:schemaLocation="http://www.springframework.org/schema/beans
			http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
			http://www.springframework.org/schema/security
			http://www.springframework.org/schema/security/spring-security-3.0.xsd">

	<beans:bean id="secu-project-properties"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer" parent="project-properties">
		<beans:property name="ignoreUnresolvablePlaceholders">
			<beans:value>false</beans:value>
		</beans:property>
		<beans:property name="locations">
			<beans:list>
				<beans:value>
					classpath*:config/security.properties
				</beans:value>
			</beans:list>
		</beans:property>
	</beans:bean>
	
	<global-method-security secured-annotations="enabled" />

	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="/index*" access="permitAll" />
		<intercept-url pattern="/login*" access="permitAll" />
		<intercept-url pattern="/login/error*" access="permitAll" />
		<intercept-url pattern="/signup*" access="permitAll" />
		<intercept-url pattern="/assets/**" access="permitAll"/>
		<intercept-url pattern="/homepage**" access="isAuthenticated() or isRememberMe()" />
		<intercept-url pattern="/**" access="permitAll"/>
		<form-login login-page="${loginFormUrl}" 
					authentication-failure-url="${authenticationFailureUrl}"
					default-target-url="${defaultTargetUrl}"
					always-use-default-target="false"/>
					
		<anonymous />
		<remember-me key="tapestwitter"/>
		<logout />
   </http>
   
   <authentication-manager alias="authenticationManager">
		<authentication-provider ref="tapestwitterAuthenticationProvider"/>
   </authentication-manager>
	
	
</beans:beans>
